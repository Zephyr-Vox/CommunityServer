# ZephyrVox server configuration, generated on first startup.
# The jwt_secret is filled with a fresh random value when this file is created.
jwt_secret = ""

[auth]
access_token_ttl = "15m"
refresh_token_ttl = "720h"
login_rate_limit = 10
registration_mode = "invite"

[server]
host = "0.0.0.0"   # 监听地址；join URL 会用它，公网部署请填实际可达域名/IP
http_port = 8745
voice_port = 8746
db_path = "./data/zephyr.db"
tls_mode = "required"          # off | optional | required
tls_cert = ""                  # PEM 证书路径；空 = 自动生成自签证书
tls_key = ""                   # PEM 私钥路径；空 = 自动生成
tls_cert_path = "./data/tls"   # 自动模式证书存储路径；与 db_path 同步调整
tls_cert_valid_years = 10      # 自动生成证书有效期（1-100）
tls_cert_extra_sans = []       # 追加 SAN（域名/IP），默认 localhost/127.0.0.1/::1

[storage]
base_dir = "./data/objects"

[avatar]
max_upload_size = 10485760  # avatar upload size limit in bytes (default 10 MiB)
max_dimension = 4096        # source image max side in pixels; rejects decompression bombs
target_size = 256           # output avatar side in pixels (square)
quality = 85                # JPEG quality, 0-100

[log]
level = "info"
path = "./data/logs"
archive_keep = 7
